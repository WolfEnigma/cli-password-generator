# cli-password-generator
A simple Python CLI tool to generate secure random passwords.
## Usage

Generate a 16-character password with all options:
- Note: Use `python password_generator.py --help` for CLI options


```bash
python password_generator.py --length 16 --uppercase --numbers --symbols
